using namespace std;
#include "Objeto.hpp"

class Car{
    private:
        int heart;
        float gasoline;
        bool isAlive;

    public:
        Car();
        /*setHeart(int heart);
        setGasoline(float gasoline);
        setIsAlive(bool isAlive);
        int getHeart();
        float gasoline();
        bool isAlive();*/
};